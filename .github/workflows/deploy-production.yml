# =============================================================================
# Production Deployment - Automated deployment to OVH Cloud
# =============================================================================
#
# This workflow:
# 1. Builds Docker images for backend and frontend
# 2. Pushes images to GitHub Container Registry
# 3. SSHs into production server
# 4. Pulls latest images and restarts services
#
# Triggered:
# - Manually via workflow_dispatch
# - Automatically on push to main (optional, commented out)

name: Deploy to Production

on:
  workflow_dispatch:
    inputs:
      deploy_backend:
        description: 'Deploy backend'
        required: true
        type: boolean
        default: true

      deploy_frontend:
        description: 'Deploy frontend'
        required: true
        type: boolean
        default: true

      run_migrations:
        description: 'Run database migrations'
        required: true
        type: boolean
        default: false

  # Uncomment to auto-deploy on push to main
  # push:
  #   branches: [main]
  #   paths:
  #     - 'crates/bdp-server/**'
  #     - 'web/**'
  #     - 'Cargo.toml'
  #     - 'Cargo.lock'
  #     - 'package.json'

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository_owner }}/bdp

jobs:
  # ===========================================================================
  # Build Backend Image
  # ===========================================================================
  build-backend:
    name: Build Backend
    runs-on: ubuntu-latest
    if: github.event.inputs.deploy_backend == 'true' || github.event_name == 'push'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-server
          tags: |
            type=sha,prefix={{branch}}-
            type=ref,event=branch
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.server
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ===========================================================================
  # Build Frontend Image
  # ===========================================================================
  build-frontend:
    name: Build Frontend
    runs-on: ubuntu-latest
    if: github.event.inputs.deploy_frontend == 'true' || github.event_name == 'push'
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-web
          tags: |
            type=sha,prefix={{branch}}-
            type=ref,event=branch
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./web
          file: ./web/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ===========================================================================
  # Deploy to Production Server
  # ===========================================================================
  deploy:
    name: Deploy to Server
    runs-on: ubuntu-latest
    needs: [build-backend, build-frontend]
    if: always() && (needs.build-backend.result == 'success' || needs.build-frontend.result == 'success' || needs.build-backend.result == 'skipped' || needs.build-frontend.result == 'skipped')

    environment:
      name: production
      url: https://${{ secrets.PRODUCTION_DOMAIN || 'your-domain.com' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.PRODUCTION_SERVER_IP }} >> ~/.ssh/known_hosts

      - name: Copy deployment files to server
        run: |
          scp -i ~/.ssh/deploy_key \
            infrastructure/deploy/docker-compose.prod.yml \
            ubuntu@${{ secrets.PRODUCTION_SERVER_IP }}:/opt/bdp/docker-compose.yml

      - name: Deploy application
        run: |
          ssh -i ~/.ssh/deploy_key ubuntu@${{ secrets.PRODUCTION_SERVER_IP }} << 'ENDSSH'
            set -e
            cd /opt/bdp

            # Login to GitHub Container Registry
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # Pull latest images
            docker compose pull

            # Run migrations if requested
            if [ "${{ github.event.inputs.run_migrations }}" == "true" ]; then
              echo "Running database migrations..."
              docker compose run --rm server sqlx migrate run
            fi

            # Restart services
            docker compose up -d --remove-orphans

            # Show status
            docker compose ps

            # Cleanup old images
            docker image prune -f

            echo "Deployment completed successfully!"
          ENDSSH

      - name: Health check
        run: |
          # Wait for services to start
          sleep 10

          # Check backend health
          if curl -f -s http://${{ secrets.PRODUCTION_SERVER_IP }}:8000/health > /dev/null; then
            echo "✅ Backend is healthy"
          else
            echo "❌ Backend health check failed"
            exit 1
          fi

      - name: Deployment summary
        run: |
          echo "## Deployment Complete 🚀" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Backend**: ${{ github.event.inputs.deploy_backend == 'true' && '✅ Deployed' || '⏭️ Skipped' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Frontend**: ${{ github.event.inputs.deploy_frontend == 'true' && '✅ Deployed' || '⏭️ Skipped' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Migrations**: ${{ github.event.inputs.run_migrations == 'true' && '✅ Ran' || '⏭️ Skipped' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Server**: \`${{ secrets.PRODUCTION_SERVER_IP }}\`" >> $GITHUB_STEP_SUMMARY
