# Enable ingestion
INGEST_ENABLED=true

# UniProt Ingestion Mode (latest or historical)
INGEST_UNIPROT_MODE=latest

# Latest Mode Configuration
INGEST_UNIPROT_CHECK_INTERVAL_SECS=86400
INGEST_UNIPROT_AUTO_INGEST=false

# For testing, we'll use a very small sample
# In production, remove or adjust these limits
